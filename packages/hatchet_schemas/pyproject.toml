[project]
name = "hatchet-schemas"
version = "0.1.0"
description = "Shared Pydantic schemas for hatchet-playground"
requires-python = ">=3.11,<3.14"
dependencies = [
    "pydantic>=2",
]

[build-system]
requires = ["uv_build>=0.10,<0.14.0"]
build-backend = "uv_build"

